# Changelog

## Unreleased

### Added

- `@bil/launchpad/shell` — `isStandaloneShell()`, `getAppContext()`, `isBilGameUrl()`, `cameFromBilGame()`, `openExternalUrl()` for PWA and Capacitor shell UX
- Analytics client attaches `$app_context` (`web` | `pwa` | `native`) on every event
